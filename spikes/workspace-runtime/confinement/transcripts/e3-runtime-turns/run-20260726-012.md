# E3 current-harness runtime turns — run-20260726-012

## Status

**PASS.** This run replaces the removed `run-20260723-002` report. The old
report used a harness option that the committed harness no longer accepts, and
the old harness was not kept. This replacement uses the committed interface.
All three coding agents completed a model turn and ran `/bin/pwd` from the
supplied workspace.

## Execution context

- Date: 2026-07-26 Asia/Singapore
- Path base: Chive repository root
- Host: outside the sandbox running this review task so macOS can apply
  Seatbelt
- Installed nono: `/opt/homebrew/Cellar/nono/0.69.0/bin/nono`
- Effective nono config: `$HOME/.config/nono`
- Harness source commit: `62c2d6960e504d841193f1f9640216e87185e948`
- Harness binary SHA-256:
  `beb42bc321a64e0d70b4eae1a35ee43a7caba8ceefb1eea327458bf694dc60c0`
- Harness Cargo project:
  `spikes/workspace-runtime/confinement/sidecar-harness/Cargo.toml`
- Session workspace:
  `tmp/confinement-runs/run-20260726-012/workspace`

Path setup, run from the Chive repository root:

```sh
CHIVE_ROOT=$PWD
RUN_ID=run-20260726-012
WORKSPACE="$CHIVE_ROOT/tmp/confinement-runs/$RUN_ID/workspace"
NONO=/opt/homebrew/Cellar/nono/0.69.0/bin/nono
HARNESS="$CHIVE_ROOT/spikes/workspace-runtime/confinement/sidecar-harness/target/debug/sidecar-harness"
```

The run uses the installed nono executable, normal nono config, and selected
packed or user profile. It does not copy nono, packs, or profiles.

## Harness verification

Commands:

```sh
cargo build \
  --manifest-path spikes/workspace-runtime/confinement/sidecar-harness/Cargo.toml \
  --bin sidecar-harness

cargo test \
  --manifest-path spikes/workspace-runtime/confinement/sidecar-harness/Cargo.toml

shasum -a 256 \
  spikes/workspace-runtime/confinement/sidecar-harness/target/debug/sidecar-harness
```

Result: **PASS.** All 15 Rust tests passed. The binary hash matches the value
recorded above. The committed interface rejects `--allow-unix-socket`, and no
runtime command below uses that removed option.

## Runtime inventory

| Runtime | Version | Selected profile | Result |
|---|---:|---|---|
| Codex | 0.145.0 | `nolabs-ai/codex` | PASS |
| Claude Code | 2.1.218 | `nolabs-ai/claude` | PASS |
| OpenCode | 1.18.4 | `opencode-0723a` | PASS |

## Host preflight

Commands ran with the process current directory set to `$WORKSPACE`:

```sh
"$NONO" setup --check-only
"$NONO" profile show nolabs-ai/codex --json >/dev/null
"$NONO" profile show nolabs-ai/claude --json >/dev/null
"$NONO" profile show opencode-0723a --json >/dev/null
```

Result: **PASS.** Nono 0.69.0 verified macOS Seatbelt filesystem and network
restrictions. All three selected profile references resolved through the
installed nono config.

## Installed inputs

These hashes bind the run to the installed nono stack that supplied the
profiles:

| Input | SHA-256 |
|---|---|
| nono executable | `c337d413c5e37868de97caba21712a18b87491670715509c19b84937c0c08695` |
| pack lockfile | `4396d0e50f484ec3c1bdbbcb4255eeb98ba2b6badf363963105b8ba77a196f8d` |
| `nolabs-ai/codex` pack profile | `83c39f094d5077ab6ecb703d16f8641ece14e7d555df382d52fdaaa57494e52c` |
| `nolabs-ai/claude` pack profile | `ac4994af3b29a0ebe6fd23a642d656d1ce3c1ff2bdada5e720279a824666159d` |
| `nolabs-ai/opencode` pack profile | `9e273d3c4a7a3766520fa597b13ba04c7a56741146d5767c8c0a7e4f21790ac2` |
| `opencode-0723a` user profile | `e7612fdbd928cef07aceec9d99efaa598fcadda61be7a9deaeef58c97050540c` |

`opencode-0723a` extends the installed OpenCode pack and adds this machine's
Bun, mise, and user-config paths. The recorded file hashes matched the installed
inputs checked for this run.

### Mutable OpenCode runtime paths

The OpenCode user profile intentionally contains mise links such as `latest`.
Their targets were checked after the model turns with
`/usr/bin/readlink <profile-path>`:

| Profile path | Observed target |
|---|---|
| `~/.local/share/mise/installs/bun/latest` | `1.3.11` |
| `~/.local/share/mise/installs/fnox/latest` | `1.25.1` |
| `~/.local/share/mise/installs/node/22` | `22.22.2` |
| `~/.local/share/mise/installs/npm-aws-azure-login/latest` | `3.6.4` |
| `~/.local/share/mise/installs/npm-aws-cdk/latest` | `2.1117.0` |
| `~/.local/share/mise/installs/npm-mariozechner-pi-coding-agent/latest` | `0.67.3` |
| `~/.local/share/mise/installs/npm-playwright/latest` | `1.58.2` |
| `~/.local/share/mise/installs/pnpm/latest` | `10.33.0` |
| `~/.local/share/mise/installs/python/3.14` | `3.14.3` |
| `~/.local/share/mise/installs/usage/latest` | `3.2.0` |
| `~/.local/share/mise/installs/uv/latest` | `0.11.2` |

The current harness does not verify those targets before launch. This E3 result
is therefore a dated compatibility snapshot, not production acceptance. #32
must resolve and record mutable profile paths before launch and make earlier
validation stale when a target changes.

## Codex 0.145.0

Command:

```sh
"$HARNESS" \
  --nono "$NONO" \
  --profile nolabs-ai/codex \
  --workspace-rw "$WORKSPACE" \
  --net open \
  -- /Users/chifeng/.local/bin/codex exec \
    --dangerously-bypass-approvals-and-sandbox \
    --ephemeral \
    --skip-git-repo-check \
    --ignore-user-config \
    --ignore-rules \
    'Use the shell to run /bin/pwd. Then reply with exactly two lines. The first line must be CWD= followed by the exact /bin/pwd output. The second line must be OK.'
```

Direct observations:

- nono verified `nolabs-ai/codex` and applied Seatbelt;
- Codex reported `workdir: $WORKSPACE` and
  `sandbox: danger-full-access`, leaving nono as the outer OS sandbox;
- session `019f9c9e-8a73-7e42-b2b5-d1aa7279316a` ran `/bin/pwd` from
  `$WORKSPACE` and the command exited 0;
- Codex replied:

  ```text
  CWD=$WORKSPACE
  OK
  ```

- the harness, nono, and Codex exited 0.

Result: **PASS.** Nono reported 11 denied optional mise path probes. They did
not affect the shell command or model turn, so this run does not add grants for
them.

## Claude Code 2.1.218

Command:

```sh
"$HARNESS" \
  --nono "$NONO" \
  --profile nolabs-ai/claude \
  --workspace-rw "$WORKSPACE" \
  --net open \
  -- /Users/chifeng/.local/bin/claude -p \
    --output-format stream-json \
    --verbose \
    --no-session-persistence \
    --dangerously-skip-permissions \
    --safe-mode \
    'Use Bash to run /bin/pwd. Then reply with exactly two lines. The first line must be CWD= followed by the exact /bin/pwd output. The second line must be OK.'
```

Direct observations:

- nono verified `nolabs-ai/claude` and applied Seatbelt;
- Claude's init event reported `cwd=$WORKSPACE`, version 2.1.218, and
  `permissionMode=bypassPermissions`;
- session `023c0339-d655-43fd-b379-8882169f5cbd` called Bash with `/bin/pwd`;
- the tool result contained `$WORKSPACE` and `is_error=false`;
- Claude replied:

  ```text
  CWD=$WORKSPACE
  OK
  ```

- the result event reported `success`, `terminal_reason=completed`, and the
  harness exited 0.

Result: **PASS.** The run did not set `CLAUDE_CODE_TMPDIR`. Nono reported denied
home, optional skill, mise, temporary-state, and old-version cleanup probes.
They did not affect the Bash call or model turn, so this run does not add grants
for them.

## OpenCode 1.18.4

Command:

```sh
"$HARNESS" \
  --nono "$NONO" \
  --profile opencode-0723a \
  --workspace-rw "$WORKSPACE" \
  --net open \
  -- /Users/chifeng/.bun/bin/opencode run \
    --format json \
    --pure \
    --auto \
    --dir "$WORKSPACE" \
    'Use the shell to run /bin/pwd. Then reply with exactly two lines. The first line must be CWD= followed by the exact /bin/pwd output. The second line must be OK.'
```

Direct observations:

- nono verified `opencode-0723a` and applied Seatbelt;
- session `ses_063604757ffexAR27CzoVD9cVF` called its Bash tool with
  `command=/bin/pwd` and `workdir=$WORKSPACE`;
- the tool output contained `$WORKSPACE`, exited 0, and was not truncated;
- OpenCode replied:

  ```text
  CWD=$WORKSPACE
  OK
  ```

- the harness, nono, and OpenCode exited 0.

Result: **PASS.** OpenCode kept its public `--dir "$WORKSPACE"` option because
OpenCode 1.18.4 otherwise prefers an inherited `PWD` value that can disagree
with the real process directory. Nono reported five denied parent, optional
skill, config, and enclosing-repository probes. They did not affect the Bash
call or model turn, so this run does not add grants for them.

## E3 result

| Runtime | Result | Required adapter rule |
|---|---|---|
| Codex 0.145.0 | **PASS** | Disable Codex's inner OS sandbox; let nono set the workspace directory. |
| Claude Code 2.1.218 | **PASS** | Let nono set the workspace directory; no temporary-directory override was needed. |
| OpenCode 1.18.4 | **PASS** | Pass OpenCode's public `--dir "$WORKSPACE"` option. |

**Overall E3: PASS.** Every counted command uses the committed harness
interface. No command uses the removed `--allow-unix-socket` option. All three
selected profiles resolved, all three runtimes completed an external model
turn, each ran `/bin/pwd` through its shell tool in the supplied workspace, and
each exited 0.

This is a compatibility result for the selected packed or user profiles. It is
not an exact-workspace filesystem result and does not replace the separate
network, credential, or one-OS-sandbox experiments.
