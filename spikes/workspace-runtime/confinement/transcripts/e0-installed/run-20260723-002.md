# E0 installed nono inventory — run-20260723-002

## Result

**PASS.** The installed nono executable, normal config store, selected packs, and
selected user profile form one coherent stack. This run did not copy or change
the executable, packs, profiles, or runtime configuration.

## Execution context

- Date: 2026-07-23 Asia/Singapore
- Path base: Chive repository root
- Host: outside the Codex sandbox when macOS applied the test Seatbelt sandbox
- Effective `HOME`: `/Users/chifeng`
- `XDG_CONFIG_HOME`: unset
- Effective nono config root: `$HOME/.config/nono`

## Host preflight

Command:

```sh
/opt/homebrew/bin/nono setup --check-only
```

Result:

- nono 0.69.0
- macOS 26.5.2
- Seatbelt filesystem restrictions: OK
- Seatbelt network restrictions: OK
- default outbound network posture: allowed

## Installed executable

Commands:

```sh
command -v nono
readlink /opt/homebrew/bin/nono
realpath /opt/homebrew/bin/nono
/opt/homebrew/bin/nono --version
shasum -a 256 /opt/homebrew/bin/nono
file /opt/homebrew/bin/nono
codesign --verify --deep --strict --verbose=2 /opt/homebrew/bin/nono
codesign -dvvv /opt/homebrew/bin/nono
```

Observed values:

| Field | Value |
|---|---|
| Discovered path | `/opt/homebrew/bin/nono` |
| Symlink target | `../Cellar/nono/0.69.0/bin/nono` |
| Canonical path | `/opt/homebrew/Cellar/nono/0.69.0/bin/nono` |
| Version | `nono 0.69.0` |
| Architecture | arm64 |
| SHA-256 | `c337d413c5e37868de97caba21712a18b87491670715509c19b84937c0c08695` |
| Code-sign verification | PASS |
| Signature | ad hoc, linker-signed |
| CDHash | `918492a3740da669ab8b4a68d59906b5ab67b47c` |

### Provenance follow-up — 2026-07-26

Commands:

```sh
jq '{built_as_bottle, poured_from_bottle, source}' \
  /opt/homebrew/Cellar/nono/0.69.0/INSTALL_RECEIPT.json
brew info --json=v2 nono
```

The code-signing check only proves that the file is structurally valid. It
does not identify the publisher. Homebrew's local install receipt and formula
metadata provide the distribution record used for this experiment:

| Field | Recorded value |
|---|---|
| Distributor | `homebrew/core` |
| Install type | Homebrew bottle, `arm64_tahoe` |
| Bottle registry | `https://ghcr.io/v2/homebrew/core` |
| Bottle SHA-256 | `534feba1a079bad7006b93613cb5f2b95591d42dffe078697eb07f74a6bae6df` |
| Upstream publisher | `nolabs-ai` |
| Upstream release | `https://github.com/nolabs-ai/nono/archive/refs/tags/v0.69.0.tar.gz` |
| Source archive SHA-256 | `eca9a027cd186e595824c99cd8bb88467a03619fb793ef7c249993fc771fe1ce` |

The installed binary SHA-256 above binds this run to the actual executable.
The Homebrew record explains where it came from; the ad hoc signature is not
treated as publisher proof.

The harness will invoke the canonical installed executable. There is no
session copy and no isolated nono config store in this run.

## Installed pack inventory

Command:

```sh
/opt/homebrew/bin/nono list --installed --json
```

The normal lockfile is
`$HOME/.config/nono/packages/lockfile.json`, SHA-256
`4396d0e50f484ec3c1bdbbcb4255eeb98ba2b6badf363963105b8ba77a196f8d`.
All three packs currently report `pinned: false`.

This is a dated experiment snapshot, not a production acceptance record. The
recorded lockfile and profile hashes identify the inputs used here. Production
must recheck them and any mutable runtime paths before every accepted launch;
an unpinned pack update makes earlier validation stale.

| Pack | Version | Minimum nono | Package SHA-256 | Profile SHA-256 | Provenance |
|---|---:|---:|---|---|---|
| `nolabs-ai/codex` | 0.2.0 | 0.64.1 | `9ac13698d42e0ede85e122d12dc11a8dece86fca3f2236254c2f54418aa9586b` | `83c39f094d5077ab6ecb703d16f8641ece14e7d555df382d52fdaaa57494e52c` | `nolabs-ai/nono-packs`, `codex-v0.2.0` |
| `nolabs-ai/claude` | 0.1.0 | 0.61.0 | `65a3be3f1ccfed3ee8e6988d12bbfac0f09ea4a803df906b6758127036d34ab5` | `ac4994af3b29a0ebe6fd23a642d656d1ce3c1ff2bdada5e720279a824666159d` | `nolabs-ai/nono-packs`, `claude-v0.1.0` |
| `nolabs-ai/opencode` | 0.1.0 | 0.55.0 | `8c171f93600c48d4e675cc3ad595916567b6cebfce4d9afa5eabfd5482a7ab02` | `9e273d3c4a7a3766520fa597b13ba04c7a56741146d5767c8c0a7e4f21790ac2` | `nolabs-ai/nono-packs`, `opencode-v0.1.0` |

## Selected profiles

Commands:

```sh
/opt/homebrew/bin/nono profile list --json
/opt/homebrew/bin/nono profile validate nolabs-ai/codex
/opt/homebrew/bin/nono profile validate nolabs-ai/claude
/opt/homebrew/bin/nono profile validate opencode-0723a
```

| Runtime | Selected reference | Resolved source | Validation | Effective network block |
|---|---|---|---|---|
| Codex | `nolabs-ai/codex` | pack `nolabs-ai/codex` | PASS, 26 group references | false |
| Claude Code | `nolabs-ai/claude` | pack `nolabs-ai/claude` | PASS, 27 group references | false |
| OpenCode | `opencode-0723a` | `$HOME/.config/nono/profiles/opencode-0723a.json` | PASS, 25 group references | false |

The OpenCode user profile has SHA-256
`e7612fdbd928cef07aceec9d99efaa598fcadda61be7a9deaeef58c97050540c`
and extends the installed `opencode` pack profile. It adds this machine's Bun,
mise, and user-config paths.

The three effective profiles have no nono profile `hooks`, `session_hooks`,
`command_args`, or `unsafe_macos_seatbelt_rules`. The installed packs have
separate wiring records in the normal lockfile; this run did not execute that
wiring.

## Disclosed filesystem and credential posture

These are selected user/pack policies, not exact-workspace policies:

- all three include `system_read_macos`, which can read broad system roots such
  as `/Volumes` and `/Applications`;
- Codex directly allows its normal `$HOME/.codex` and `$HOME/.agents` state;
- Claude Code directly allows its normal state plus
  `$HOME/Library/Keychains` read-write with deny bypass;
- OpenCode directly allows its normal state, `$TMPDIR`, and the user profile's
  local runtime/config paths; it can read
  `$HOME/Library/Keychains/login.keychain-db` with deny bypass.

This breadth is accepted for the rev-12 compatibility experiment and remains
visible as a hardening risk. It must not be described as exact-workspace
confinement.

## E0 conclusion

The selected stack is ready for E3:

```text
/opt/homebrew/Cellar/nono/0.69.0/bin/nono
  + $HOME/.config/nono
  + nolabs-ai/codex
  + nolabs-ai/claude
  + opencode-0723a → nolabs-ai/opencode
```

Any executable, lockfile, pack profile, or user-profile hash change invalidates
this inventory and requires the relevant validation to be repeated.
