# E4 Phase 0 proxy behavior — run-20260725-005

## Status

**PASS.** The current explicit-domain command started nono's filtering proxy,
allowed a named host, denied an unnamed host, prevented a direct-network
bypass, and activated HTTPS path checking only when a path rule required it.

## Purpose

Rerun E4 Phase 0 using the current Chive network rules:

- use repeated `--allow-domain` values only;
- do not pass `--network-profile`;
- do not pass Unix-socket flags;
- record what nono injects without copying proxy tokens or private CA data.

This phase checks nono's proxy behavior with ordinary shell commands. It does
not run Codex, Claude Code, or OpenCode; the agent runs belong to later E4
phases.

## Execution context

- Date: 2026-07-25 Asia/Singapore
- Path base: Chive repository root
- Installed nono: `/opt/homebrew/bin/nono`
- Resolved nono: `/opt/homebrew/Cellar/nono/0.69.0/bin/nono`
- Version: 0.69.0
- SHA-256: `c337d413c5e37868de97caba21712a18b87491670715509c19b84937c0c08695`
- Selected runtime profile: `nolabs-ai/codex`
- Selected profile network settings: network allowed, no network profile, no
  allowed domains
- Run workspace: `tmp/confinement-runs/run-20260725-005/workspace`

## Host preflight

The first check ran inside this task's existing macOS sandbox and stopped with:

```text
sandbox initialization failed: Operation not permitted
```

Per ground rule 0, that attempt produced no experiment result. The approved
host command was:

```sh
/opt/homebrew/bin/nono setup --check-only
```

Result: **PASS.** Nono verified macOS Seatbelt, filesystem restrictions, and
network restrictions on macOS 26.5.2.

## Plain hostname rule

Command, run from the Chive repository root on the approved host. The child
script is wrapped across lines here for readability:

```sh
/opt/homebrew/bin/nono run -vv \
  --diagnostics-json \
  --log-file tmp/confinement-runs/run-20260725-005/phase0-plain.log \
  --profile nolabs-ai/codex \
  -a tmp/confinement-runs/run-20260725-005/workspace \
  --workdir tmp/confinement-runs/run-20260725-005/workspace \
  --allow-domain example.com \
  --no-rollback \
  -- /bin/sh -c '
    for name in HTTP_PROXY HTTPS_PROXY http_proxy https_proxy NO_PROXY \
      NONO_NO_PROXY NONO_PROXY_TOKEN NODE_USE_ENV_PROXY SSL_CERT_FILE \
      REQUESTS_CA_BUNDLE NODE_EXTRA_CA_CERTS CURL_CA_BUNDLE GIT_SSL_CAINFO
    do
      if /usr/bin/printenv "$name" >/dev/null; then
        echo "ENV $name=set"
      else
        echo "ENV $name=unset"
      fi
    done
    /usr/bin/curl --fail --silent --show-error --head --max-time 15 \
      https://example.com
  '
```

The quoted child command in the executed call checked these names without
printing their values:

```text
HTTP_PROXY HTTPS_PROXY http_proxy https_proxy NO_PROXY NONO_NO_PROXY
NONO_PROXY_TOKEN NODE_USE_ENV_PROXY SSL_CERT_FILE REQUESTS_CA_BUNDLE
NODE_EXTRA_CA_CERTS CURL_CA_BUNDLE GIT_SSL_CAINFO
```

Result: **PASS.** Nono assigned proxy port 53961. The child received all
eight proxy-related environment names. The five CA-related names were unset.
Nono logged `mode=connect` for `CONNECT example.com:443`, curl returned HTTP
200, and the session exited 0. Nono also reported that no route required
reading HTTPS contents, so it did not generate a CA.

The verbose log also showed a request to `registry.nono.sh` while nono verified
the installed pack before starting the sandboxed child. That is nono setup
traffic, not traffic from the child command.

## Host outside the allowlist

Command:

```sh
/opt/homebrew/bin/nono run -vv \
  --diagnostics-json \
  --log-file tmp/confinement-runs/run-20260725-005/phase0-denied.log \
  --profile nolabs-ai/codex \
  -a tmp/confinement-runs/run-20260725-005/workspace \
  --workdir tmp/confinement-runs/run-20260725-005/workspace \
  --allow-domain example.com \
  --no-rollback \
  -- /usr/bin/curl --fail --silent --show-error --head --max-time 15 \
    https://www.iana.org
```

Result: **PASS as a denial.** Nono assigned proxy port 53998, logged
`mode=connect` with a denied decision for `www.iana.org:443`, and returned HTTP
403. Curl and nono exited 22. No filesystem denial was involved.

## HTTPS path rule

Command. The child script is wrapped across lines here for readability:

```sh
/opt/homebrew/bin/nono run -vv \
  --diagnostics-json \
  --log-file tmp/confinement-runs/run-20260725-005/phase0-path.log \
  --profile nolabs-ai/codex \
  -a tmp/confinement-runs/run-20260725-005/workspace \
  --workdir tmp/confinement-runs/run-20260725-005/workspace \
  --allow-domain 'https://example.com/**' \
  --no-rollback \
  -- /bin/sh -c '
    for name in SSL_CERT_FILE REQUESTS_CA_BUNDLE NODE_EXTRA_CA_CERTS \
      CURL_CA_BUNDLE GIT_SSL_CAINFO
    do
      if /usr/bin/printenv "$name" >/dev/null; then
        echo "ENV $name=set"
      else
        echo "ENV $name=unset"
      fi
    done
    /usr/bin/curl --fail --silent --show-error --head --max-time 15 \
      https://example.com
  '
```

Result: **PASS.** Nono assigned proxy port 54022, created a temporary trust
bundle, gave the child read access to it, and set all five CA environment
names. It logged `mode=connect_intercept`, checked `HEAD /` against the path
rule, returned HTTP 200, and exited 0. The trust bundle and its session
directory were gone after the process exited. No certificate or private key
content was copied into this report.

## Direct-network bypass

Command:

```sh
/opt/homebrew/bin/nono run -vv \
  --diagnostics-json \
  --log-file tmp/confinement-runs/run-20260725-005/phase0-direct-bypass.log \
  --profile nolabs-ai/codex \
  -a tmp/confinement-runs/run-20260725-005/workspace \
  --workdir tmp/confinement-runs/run-20260725-005/workspace \
  --allow-domain example.com \
  --no-rollback \
  -- /usr/bin/curl --noproxy '*' --fail --silent --show-error --head \
    --max-time 15 https://example.com
```

Result: **PASS as a denial.** Nono assigned proxy port 54091. Curl was told to
ignore the injected proxy and its direct connection failed with exit 7. The
proxy log recorded no accepted child connection. Together with the successful
plain-host check, this proves that the sandboxed child could connect to nono's
assigned localhost proxy but could not bypass it for a direct remote
connection.

## Phase 0 result

| Check | Result |
|---|---|
| Host preflight | PASS |
| Plain allowed hostname | PASS |
| Host outside the allowlist | PASS as a denial |
| HTTPS path rule | PASS |
| Direct-network bypass | PASS as a denial |

**E4 Phase 0: PASS.** This result covers nono's proxy mechanism with shell
commands under the selected Codex runtime profile. It does not change overall
E4 from `PENDING`: the rev-18 harness change and the three coding-agent runs
still remain.

## Cleanup

The temporary workspace and four verbose logs were stored only under
`tmp/confinement-runs/run-20260725-005`. Cleanup removed that exact run
directory after the results were recorded. Nono removed its own temporary
HTTPS inspection trust bundle when the path-rule session ended. A host process
list check found no remaining nono or `run-20260725-005` process.
