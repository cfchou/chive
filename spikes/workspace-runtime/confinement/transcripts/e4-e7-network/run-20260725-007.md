# E4 Phase 2 network choices — run-20260725-007

## Status

**PASS.** Codex, Claude Code, and OpenCode each passed all three Chive network
choices:

| Choice | What the agent's shell proved |
|---|---|
| `open` | `example.com` returned HTTP 200 |
| `agent-only` | the model provider stayed available, while `example.com` returned HTTP 403 |
| `restricted` | `example.com` returned HTTP 200, while `www.iana.org` returned HTTP 403 |

This completes E4. These tests instructed each coding agent to run the shell
commands. They did not run standalone curls beside the agent.

## Execution context

- Date: 2026-07-25 Asia/Singapore
- Path base: Chive repository root
- Host: outside this task's existing sandbox, so nono could apply macOS
  Seatbelt
- Installed nono: `/opt/homebrew/bin/nono` 0.69.0
- E0 canonical target:
  `/opt/homebrew/Cellar/nono/0.69.0/bin/nono`, SHA-256
  `c337d413c5e37868de97caba21712a18b87491670715509c19b84937c0c08695`
- Harness project:
  `spikes/workspace-runtime/confinement/sidecar-harness/Cargo.toml`
- Workspace: `tmp/confinement-runs/run-20260725-007/workspace`

| Runtime | Profile | Provider hosts explicitly allowed in limited modes |
|---|---|---|
| Codex 0.145.0 | `nolabs-ai/codex` | `chatgpt.com` |
| Claude Code 2.1.218 | `nolabs-ai/claude` | `api.anthropic.com`, `platform.claude.com` |
| OpenCode 1.18.4 | `opencode-0723a` | `api.githubcopilot.com` |

The provider lists came from Phase 1. `platform.claude.com` was kept because
Claude used it in the first Phase 1 model turn, even though it did not contact
that host in Phase 2. Optional telemetry and configured tools were not added:
their denials did not stop the model turns.

## Preflight and command setup

The host preflight passed before the run:

```sh
/opt/homebrew/bin/nono setup --check-only
```

The commands below replace repeated absolute paths with variables. Run them
from the Chive repository root.

```sh
CHIVE_ROOT=$PWD
RUN_ID=run-20260725-007
RUN_ROOT="$CHIVE_ROOT/tmp/confinement-runs/$RUN_ID"
WORKSPACE="$RUN_ROOT/workspace"
NONO=/opt/homebrew/bin/nono
HARNESS="$CHIVE_ROOT/spikes/workspace-runtime/confinement/sidecar-harness/target/debug/sidecar-harness"
```

The E4 commands used the mutable `/opt/homebrew/bin/nono` link and did not
recheck its canonical target and hash before every invocation. This report is
therefore dated experiment evidence, not a production acceptance record.
Production and future acceptance runs must canonicalize the configured path
and verify its recorded hash before launch.

Every harness call used `--workspace-rw "$WORKSPACE"`. The harness turns that
into nono's `-a "$WORKSPACE"` read-write grant and
`--workdir "$WORKSPACE"`. No command passed `--network-profile` or a Unix
socket option.

## Agent shell instructions

The commands below include each prompt exactly as nono recorded it. The small
wording differences reflect each agent's shell-tool name. All requests used
curl's `--fail`, `--head`, and 15-second timeout.

## Codex

Executed harness calls:

```sh
"$HARNESS" --nono "$NONO" --profile nolabs-ai/codex \
  --workspace-rw "$WORKSPACE" --net open -- \
  /Users/chifeng/.local/bin/codex exec \
    --dangerously-bypass-approvals-and-sandbox \
    --ephemeral --skip-git-repo-check \
    --ignore-user-config --ignore-rules \
    'Use the shell once to run: /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://example.com . If curl exits 0, reply with exactly ALLOWED. Otherwise reply with exactly FAILED.'

"$HARNESS" --nono "$NONO" --profile nolabs-ai/codex \
  --workspace-rw "$WORKSPACE" --net agent-only \
  --allow-domain chatgpt.com -- \
  /Users/chifeng/.local/bin/codex exec \
    --dangerously-bypass-approvals-and-sandbox \
    --ephemeral --skip-git-repo-check \
    --ignore-user-config --ignore-rules \
    'Use the shell once. Run exactly this command without adding arguments: /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://example.com
If curl exits nonzero because the request is denied, reply with exactly BLOCKED. If curl exits 0, reply with exactly ALLOWED.'

"$HARNESS" --nono "$NONO" --profile nolabs-ai/codex \
  --workspace-rw "$WORKSPACE" --net restricted \
  --allow-domain chatgpt.com \
  --allow-domain example.com -- \
  /Users/chifeng/.local/bin/codex exec \
    --dangerously-bypass-approvals-and-sandbox \
    --ephemeral --skip-git-repo-check \
    --ignore-user-config --ignore-rules \
    'Use the shell to run these two commands separately: (1) /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://example.com (2) /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://www.iana.org . Then reply with exactly two lines based on their exit status: example.com=ALLOWED or example.com=BLOCKED; www.iana.org=ALLOWED or www.iana.org=BLOCKED.'
```

| Choice | Agent reply | Audit session | Result |
|---|---|---|---|
| open | `ALLOWED` | `236bd04908863559` | example HTTP 200 |
| agent-only | `BLOCKED` | `55152af2b5f155dd` | provider allowed; example denied |
| restricted | `example.com=ALLOWED`<br>`www.iana.org=BLOCKED` | `843e8f68bc53c1be` | example allowed; IANA denied |

The first agent-only attempt used a prompt with a separate period after the
URL. Codex also tried to resolve `.`. Session `caade216c7b77faf` is preserved
but does not count. The corrected command above produced the counted result.

## Claude Code

Executed harness calls:

```sh
"$HARNESS" --nono "$NONO" --profile nolabs-ai/claude \
  --workspace-rw "$WORKSPACE" --net open -- \
  /Users/chifeng/.local/bin/claude -p \
    --output-format stream-json --verbose \
    --no-session-persistence \
    --dangerously-skip-permissions --safe-mode \
    'Use the Bash tool once to run: /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://example.com . If curl exits 0, reply with exactly ALLOWED. Otherwise reply with exactly FAILED.'

"$HARNESS" --nono "$NONO" --profile nolabs-ai/claude \
  --workspace-rw "$WORKSPACE" --net agent-only \
  --allow-domain api.anthropic.com \
  --allow-domain platform.claude.com -- \
  /Users/chifeng/.local/bin/claude -p \
    --output-format stream-json --verbose \
    --no-session-persistence \
    --dangerously-skip-permissions --safe-mode \
    'Use the Bash tool once. Run exactly this command without adding arguments: /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://example.com
If curl exits nonzero because the request is denied, reply with exactly BLOCKED. If curl exits 0, reply with exactly ALLOWED.'

"$HARNESS" --nono "$NONO" --profile nolabs-ai/claude \
  --workspace-rw "$WORKSPACE" --net restricted \
  --allow-domain api.anthropic.com \
  --allow-domain platform.claude.com \
  --allow-domain example.com -- \
  /Users/chifeng/.local/bin/claude -p \
    --output-format stream-json --verbose \
    --no-session-persistence \
    --dangerously-skip-permissions --safe-mode \
    'Use the Bash tool to run these two commands separately: (1) /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://example.com (2) /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://www.iana.org
Then reply with exactly two lines based on their exit status: example.com=ALLOWED or example.com=BLOCKED; www.iana.org=ALLOWED or www.iana.org=BLOCKED.'
```

| Choice | Agent reply | Audit session | Result |
|---|---|---|---|
| open | `ALLOWED` | `2abc18a9e68b6d06` | example HTTP 200 |
| agent-only | `BLOCKED` | `4265bb276708d9a0` | provider allowed; example denied |
| restricted | `example.com=ALLOWED`<br>`www.iana.org=BLOCKED` | `fcd6f9fc053d2bcf` | example allowed; IANA denied |

The run did not set `CLAUDE_CODE_TMPDIR`. The selected packed profile's
existing `/private/tmp` access was enough.

## OpenCode

Executed harness calls:

```sh
"$HARNESS" --nono "$NONO" --profile opencode-0723a \
  --workspace-rw "$WORKSPACE" --net open -- \
  /Users/chifeng/.bun/bin/opencode run \
    --format json --pure --auto \
    --dir "$WORKSPACE" \
    'Use the shell tool once. Run exactly this command without adding arguments: /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://example.com
If curl exits 0, reply with exactly ALLOWED. Otherwise reply with exactly FAILED.'

"$HARNESS" --nono "$NONO" --profile opencode-0723a \
  --workspace-rw "$WORKSPACE" --net agent-only \
  --allow-domain api.githubcopilot.com -- \
  /Users/chifeng/.bun/bin/opencode run \
    --format json --pure --auto \
    --dir "$WORKSPACE" \
    'Use the shell tool once. Run exactly this command without adding arguments: /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://example.com
If curl exits nonzero because the request is denied, reply with exactly BLOCKED. If curl exits 0, reply with exactly ALLOWED.'

"$HARNESS" --nono "$NONO" --profile opencode-0723a \
  --workspace-rw "$WORKSPACE" --net restricted \
  --allow-domain api.githubcopilot.com \
  --allow-domain example.com -- \
  /Users/chifeng/.bun/bin/opencode run \
    --format json --pure --auto \
    --dir "$WORKSPACE" \
    'Use the shell tool to run these two commands separately: (1) /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://example.com (2) /usr/bin/curl --fail --silent --show-error --head --max-time 15 https://www.iana.org
Then reply with exactly two lines based on their exit status: example.com=ALLOWED or example.com=BLOCKED; www.iana.org=ALLOWED or www.iana.org=BLOCKED.'
```

| Choice | Agent reply | Audit session | Result |
|---|---|---|---|
| open | `ALLOWED` | `4b975100c1277c65` | example HTTP 200 |
| agent-only | `BLOCKED` | `d05242eaf105e5c4` | provider allowed; example denied |
| restricted | `example.com=ALLOWED`<br>`www.iana.org=BLOCKED` | `0d56ca721b2b7a89` | example allowed; IANA denied |

OpenCode used GitHub Copilot. Its configured Exa MCP connection was denied in
both limited modes, but the model turns and requested shell commands still
finished. OpenCode kept its explicit `--dir` because that is its public
workspace interface.

## Audit checks

All nine counted nono sessions exited 0. For each session, this command passed:

```sh
"$NONO" audit verify <session-id> --json
```

Each result reported matching event counts, verified records, a matching
session digest, and a verified audit ledger chain. The sessions did not carry
an optional signed attestation; that does not make their audit checks fail.

The sanitized event counts are in
`phase2-audit.run-20260725-007.json`. Open mode does not use nono's proxy, so
its audit record contains no proxy network events. The agent shell output is
the evidence that the open request returned HTTP 200.

## Result

E4 is **PASS**:

- `open` allowed model traffic and agent tool traffic;
- `agent-only` kept each tested provider online and denied unrelated agent
  tool traffic;
- `restricted` added the reviewed `example.com` host and still denied
  `www.iana.org`;
- every limited-mode runtime used explicit `--allow-domain` values, with no
  hidden named network profile.

This result applies to the tested runtime versions, selected profiles, and
provider choices. OpenCode's provider can change, so its provider host list
must be rebuilt when its configuration changes.
