# E4 Phase 1 endpoint observations — run-20260725-006

## Status

**PASS.** Codex, Claude Code, and OpenCode each completed one local account
check and two model turns in a row. Every command exited 0, every model turn
replied `OK`, and the loopback observation proxy recorded the hosts used by
the model turns.

This completes E4 Phase 1 only. E4 remains `PENDING` until Phase 2 proves
`open`, `agent-only`, and `restricted` with explicit host lists.

## What this phase tests

This phase observes network destinations. It does not deny any host. Each
runtime ran inside its selected nono filesystem profile with Chive's
`--net open` choice. A separate nono proxy listened only on localhost and
recorded the destination hostname for each HTTPS connection.

The observed hosts apply to the exact commands, installed versions, profiles,
and user configuration in this run. They are not a complete or minimal list.
Login refresh, another provider, or another runtime configuration can use
other hosts.

## Execution context

- Date: 2026-07-25 Asia/Singapore
- Path base: Chive repository root
- Host: outside this task's existing sandbox, so nono could apply macOS
  Seatbelt
- Installed nono: `/opt/homebrew/bin/nono` 0.69.0
- Harness project:
  `spikes/workspace-runtime/confinement/sidecar-harness/Cargo.toml`
- Workspace: `tmp/confinement-runs/run-20260725-006/workspace`
- Observation proxy: `127.0.0.1:54181`, no authentication, available only
  during this run

| Runtime | Profile |
|---|---|
| Codex 0.145.0 | `nolabs-ai/codex` |
| Claude Code 2.1.218 | `nolabs-ai/claude` |
| OpenCode 1.18.4 | `opencode-0723a` |

## Harness change before the run

Rev 18 required Chive to stop accepting nono's broad `--network-profile`
shortcut. The public command test was added first and failed because the
harness still accepted the option. The harness then removed the option and
made both limited modes require at least one explicit `--allow-domain`.

Commands, run from
`spikes/workspace-runtime/confinement/sidecar-harness`:

```sh
cargo test --test command network_profile_option_is_rejected -- --exact
cargo test --test command
cargo fmt --check
```

The first command failed before the code change. After the change, all 15
command tests passed and formatting was clean.

## Host preflight

```sh
/opt/homebrew/bin/nono setup --check-only
```

Result: **PASS.** Nono verified macOS Seatbelt, filesystem restrictions, and
network restrictions on macOS 26.5.2. All three selected profiles also
resolved through the installed nono configuration.

## Command setup

The commands below preserve the executed arguments while replacing the Chive
root and repeated paths with variables. Run them from the Chive repository
root.

```sh
CHIVE_ROOT=$PWD
RUN_ID=run-20260725-006
RUN_ROOT="$CHIVE_ROOT/tmp/confinement-runs/$RUN_ID"
WORKSPACE="$RUN_ROOT/workspace"
NONO=/opt/homebrew/bin/nono
HARNESS="$CHIVE_ROOT/spikes/workspace-runtime/confinement/sidecar-harness/target/debug/sidecar-harness"
PROXY=http://127.0.0.1:54181
```

The observation proxy command was:

```sh
"$NONO" proxy -vv \
  --listen 127.0.0.1 \
  --port 54181 \
  --no-auth \
  --log-file "$RUN_ROOT/phase1-proxy.log"
```

Each runtime command used this harness shape:

```sh
"$HARNESS" \
  --nono "$NONO" \
  --profile <selected-profile> \
  --workspace-rw "$WORKSPACE" \
  --net open \
  -- /usr/bin/env \
    HTTP_PROXY="$PROXY" \
    HTTPS_PROXY="$PROXY" \
    http_proxy="$PROXY" \
    https_proxy="$PROXY" \
    NODE_USE_ENV_PROXY=1 \
    <runtime-command>
```

No `--network-profile` or Unix-socket option was passed.

## Codex

Profile: `nolabs-ai/codex`.

The three `<runtime-command>` values, in order, were:

```sh
/Users/chifeng/.local/bin/codex login status

/Users/chifeng/.local/bin/codex exec \
  --dangerously-bypass-approvals-and-sandbox \
  --ephemeral \
  --skip-git-repo-check \
  --ignore-user-config \
  --ignore-rules \
  'Reply with exactly OK'

/Users/chifeng/.local/bin/codex exec \
  --dangerously-bypass-approvals-and-sandbox \
  --ephemeral \
  --skip-git-repo-check \
  --ignore-user-config \
  --ignore-rules \
  'Reply with exactly OK'
```

| Case | Exit | Reply | Observed hosts |
|---|---:|---|---|
| Login status | 0 | Logged in | none |
| First model turn | 0 | `OK` | `chatgpt.com`, `ab.chatgpt.com` |
| Second immediate model turn | 0 | `OK` | `chatgpt.com`, `ab.chatgpt.com` |

Codex reported the supplied workspace as its working directory. Optional mise
metadata reads were denied by the filesystem profile, but both turns finished.

## Claude Code

Profile: `nolabs-ai/claude`.

The three `<runtime-command>` values were:

```sh
/Users/chifeng/.local/bin/claude auth status

/Users/chifeng/.local/bin/claude -p \
  --output-format stream-json \
  --verbose \
  --no-session-persistence \
  --dangerously-skip-permissions \
  --safe-mode \
  'Reply with exactly OK'

/Users/chifeng/.local/bin/claude -p \
  --output-format stream-json \
  --verbose \
  --no-session-persistence \
  --dangerously-skip-permissions \
  --safe-mode \
  'Reply with exactly OK'
```

| Case | Exit | Reply | Observed hosts |
|---|---:|---|---|
| Auth status | 0 | Logged in | none |
| First model turn | 0 | `OK` | `api.anthropic.com`, `platform.claude.com`, `http-intake.logs.us5.datadoghq.com` |
| Second immediate model turn | 0 | `OK` | `api.anthropic.com`, `http-intake.logs.us5.datadoghq.com` |

The run did not set `CLAUDE_CODE_TMPDIR`. Claude reported the supplied
workspace as its current directory and finished both turns. The selected
profile's existing `/private/tmp` access was enough for these commands.

## OpenCode

Profile: `opencode-0723a`.

The three `<runtime-command>` values were:

```sh
/Users/chifeng/.bun/bin/opencode auth list --pure

/Users/chifeng/.bun/bin/opencode run \
  --format json \
  --pure \
  --auto \
  --dir "$WORKSPACE" \
  'Reply with exactly OK'

/Users/chifeng/.bun/bin/opencode run \
  --format json \
  --pure \
  --auto \
  --dir "$WORKSPACE" \
  'Reply with exactly OK'
```

| Case | Exit | Reply | Observed hosts |
|---|---:|---|---|
| Auth list | 0 | Local credentials listed | none |
| First model turn | 0 | `OK` | `api.githubcopilot.com`, `mcp.exa.ai` |
| Second immediate model turn | 0 | `OK` | `api.githubcopilot.com`, `mcp.exa.ai` |

The response metadata identified GitHub Copilot as the model provider. Opaque
provider metadata and credential details were not saved. The Exa host came
from configured MCP startup traffic. `models.dev`, seen in the older run, did
not appear this time.

## Result and Phase 2 input

| Runtime | Account check | First model turn | Second immediate model turn |
|---|---|---|---|
| Codex 0.145.0 | PASS | PASS | PASS |
| Claude Code 2.1.218 | PASS | PASS | PASS |
| OpenCode 1.18.4 | PASS | PASS | PASS |

**E4 Phase 1: PASS.** The sanitized host record is
[`phase1-proxy.run-20260725-006.json`](phase1-proxy.run-20260725-006.json).

Phase 2 should begin with these observed provider hosts:

```text
Codex        chatgpt.com
Claude Code  api.anthropic.com, platform.claude.com
OpenCode     api.githubcopilot.com
```

`ab.chatgpt.com`, Datadog logging, and configured Exa MCP startup traffic are
observed optional traffic until Phase 2 proves otherwise. Phase 2 must record
its exact final list and add a host if an agent cannot finish without it.

## Cleanup

The observation proxy was stopped normally. A host process check found no
remaining nono proxy or agent process. The raw proxy log and empty workspace
were kept only under `tmp/confinement-runs/run-20260725-006` until this
sanitized report was written, then that exact run directory was removed.
